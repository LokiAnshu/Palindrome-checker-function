# PALINDROME CHECKER FUNCTION
This is a function used to determine whether the string is a palindrome or not

# Features
  - Can identify all types of palindromes
  - Ignores pharses with spaces or any symbol (for ex , . : " etc)

# How to run
  - Use the syntax "check()" to run the function
